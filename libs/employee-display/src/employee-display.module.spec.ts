import { EmployeeDisplayModule } from './employee-display.module';

describe('EmployeeDisplayModule', () => {
  it('should work', () => {
    expect(new EmployeeDisplayModule()).toBeDefined();
  });
});
